backupvsc () {
  if [ `uname` = 'Darwin' ] ; then
    vs --list-extensions > ~/dotfiles/zfiles/vscodevscode.list
  fi
}

restorevsc () {
  if [ `uname` = 'Darwin' ] ; then
    for x in $(cat ~/dotfiles/zfiles/vscode/vscode.list); do code --install-extension $x; done
  fi
}
